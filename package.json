{
  "name": "cloud-flex",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve:prod": "npm run build && serve -p 2000 ./dist",
    "publish": "npm run build && git -C ./dist add . && git -C dist commit -m \"dist: build\" && git -C ./dist push"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "sass-embedded": "^1.83.1",
    "vite": "^6.0.5",
    "vite-plugin-handlebars": "^2.0.0"
  }
}
