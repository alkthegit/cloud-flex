{
  "name": "cloud-flex",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve:prod": "npm run build && serve -p 2000 ./dist",
    "publish": "git commit -am \"dist: build\" && git push"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "sass-embedded": "^1.83.1",
    "vite": "^6.0.5"
  }
}
